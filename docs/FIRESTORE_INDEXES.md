---
liquid: false
---

# Firestore 인덱스 설정 가이드

## 📌 중요: 컬렉션/필드 등록은 필요 없습니다!

Firestore는 **NoSQL 데이터베이스**이므로:
- ✅ 컬렉션을 미리 만들 필요 없음 (자동 생성)
- ✅ 필드(컬럼)를 미리 정의할 필요 없음 (자동 생성)
- ❌ 하지만 **복합 쿼리**를 사용할 때는 **인덱스**가 필요함

---

## 🔍 현재 프로젝트에서 필요한 인덱스

### 1. 게시글 (posts) 컬렉션

#### 인덱스 1: 카테고리별 조회 (팀 없을 때)
```
컬렉션: posts
필드:
  - category (오름차순)
  - createdAt (내림차순)
```

**사용 쿼리:**
```javascript
where('category', '==', 'records')
orderBy('createdAt', 'desc')
```

#### 인덱스 2: 카테고리 + 팀별 조회 (팀 있을 때) ⚠️ 필수
```
컬렉션: posts
필드:
  - category (오름차순)
  - team (오름차순)
  - createdAt (내림차순)
```

**사용 쿼리:**
```javascript
where('category', '==', 'records')
where('team', '==', 'egypt')
orderBy('createdAt', 'desc')
```

### 2. 카테고리 (categories) 컬렉션

#### 인덱스 3: 정렬 순서별 조회
```
컬렉션: categories
필드:
  - order (오름차순)
```

**사용 쿼리:**
```javascript
orderBy('order', 'asc')
```

---

## 🛠️ 인덱스 생성 방법

### 방법 1: 자동 생성 (권장)

1. 앱에서 해당 쿼리를 실행
2. Firebase Console에서 에러 메시지 확인
3. 에러 메시지에 **"인덱스 생성"** 링크 클릭
4. 자동으로 인덱스 생성 페이지로 이동
5. "인덱스 만들기" 클릭

### 방법 2: 수동 생성

1. Firebase Console 접속
2. **Firestore Database** > **인덱스** 탭 클릭
3. **인덱스 만들기** 클릭
4. 다음 정보 입력:

#### 인덱스 1: posts - category + createdAt
- 컬렉션 ID: `posts`
- 필드 추가:
  - `category` (오름차순)
  - `createdAt` (내림차순)
- 쿼리 범위: 컬렉션
- **만들기** 클릭

#### 인덱스 2: posts - category + team + createdAt ⚠️ 필수
- 컬렉션 ID: `posts`
- 필드 추가:
  - `category` (오름차순)
  - `team` (오름차순)
  - `createdAt` (내림차순)
- 쿼리 범위: 컬렉션
- **만들기** 클릭

#### 인덱스 3: categories - order
- 컬렉션 ID: `categories`
- 필드 추가:
  - `order` (오름차순)
- 쿼리 범위: 컬렉션
- **만들기** 클릭

---

## ⏱️ 인덱스 생성 시간

- 소규모 데이터: **몇 초 ~ 1분**
- 대규모 데이터: **몇 분 ~ 수십 분**

인덱스 생성 중에는 해당 쿼리를 사용할 수 없습니다.

---

## ✅ 인덱스 생성 확인

1. Firebase Console > Firestore Database > **인덱스** 탭
2. 생성된 인덱스 목록 확인
3. 상태가 **"사용 가능"**이면 완료

---

## 🚨 에러 메시지 예시

인덱스가 없을 때 나타나는 에러:

```
Error: The query requires an index. 
You can create it here: https://console.firebase.google.com/...
```

이 링크를 클릭하면 자동으로 인덱스 생성 페이지로 이동합니다.

---

## 📝 참고사항

### 단일 필드 쿼리는 인덱스 불필요
```javascript
// 인덱스 불필요
where('category', '==', 'records')

// 인덱스 불필요
orderBy('createdAt', 'desc')
```

### 복합 쿼리는 인덱스 필요
```javascript
// 인덱스 필요: where + orderBy
where('category', '==', 'records')
orderBy('createdAt', 'desc')

// 인덱스 필요: 여러 where + orderBy
where('category', '==', 'records')
where('team', '==', 'egypt')
orderBy('createdAt', 'desc')
```

---

## 🎯 요약

1. **컬렉션/필드 등록 불필요** - 자동 생성됨
2. **복합 쿼리 인덱스 필요** - Firebase Console에서 생성
3. **에러 메시지 링크 활용** - 가장 쉬운 방법
4. **인덱스 생성 대기** - 생성 완료까지 시간 필요

